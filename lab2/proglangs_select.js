

function printToOutputBox(selectobj) {
var e_proglangs = document.getElementById("proglangs");
var e_outputbox = document.getElementById("outputbox");
e_outputbox.innerHTML = "Du har valgt programmeringsspråket " + e_proglangs.		// Endrer innholdet i outputboxen
options[selectobj.selectedIndex].text;

}

function myFunction()
{
alert("Beklager, denne siden er under konstruksjon!");				// Gjør at myFunction sender et varsel.
}
